First install the version of R in R_install_executable if you don't have R already

Open R and install packages: "EcoHydRology" and "htmlTable"
-make sure these are installed in the default library
-if it gives error that the library is "not writeable" and asks to install in personal library, DO NOT; close R, right click the icon and "run as Administrator", then install the packages

Open up WinPython Control Panel.exe

Go to Advanced > Register Distribution (click "yes")

Open up "Spyder.exe"

Do good stuff.